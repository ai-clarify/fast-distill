name: Test

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    types:
      - opened
      - synchronize
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Lint
        run: |
          uvx ruff@0.8.1 check src/fastdistill tests
          uvx ruff@0.8.1 format --check src/fastdistill tests

  tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
          cache-dependency-glob: pyproject.toml

      # Lightweight extras only: API clients + test infra.
      # Heavy ML deps (torch, vllm, outlines, llama-cpp, sentence-transformers,
      # faiss, ray, mlx, text-clustering) are excluded — tests that need them
      # use `pytest.importorskip` and will be skipped gracefully.
      - name: Install dependencies
        run: |
          uv pip install --system -e ".[tests,anthropic,argilla,cohere,groq,hf-inference-endpoints,litellm,ollama,openai,mistralai,instructor,minhash,vision,faiss-cpu]"
          python -m nltk.downloader punkt_tab

      - name: Unit Tests
        run: pytest -n auto tests/unit

      - name: Integration Tests
        run: pytest tests/integration
